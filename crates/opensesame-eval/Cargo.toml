[package]
name        = "opensesame-eval"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Evaluation: PESQ, STOI, SI-SNR, WER, codec ABX — all implemented from scratch"

[dependencies]
# Pure Rust implementation — no external crate dependencies beyond workspace path crates
atlas-core   = { path = "../atlas-core" }
